
# === 2:导出邀约酒店名单、导入集团意向酒店名单增加酒店ID ===
## 需求描述
需求标题：导出邀约酒店名单、导入集团意向酒店名单增加酒店ID
具体描述：
1. 使用一个存在指定存在酒店的项目，进入到该签约的修改项目编辑页面
2. 在邀请酒店栏中普通单店的页面下，通过数据库的表获取数据：并且需要联合HTL_INFO.T_PROJECT_INTENT_HOTEL
   - 这是oracle数据库的表，需要根据PROJECT_ID和INVITE_STATUE=1，并且需要联合HTL_INFO.T_HOTEL表中的t.HOTELID, 获取酒店名t.CHN_NAME
3. 另外需要在集团意向单店页面中，通过数据库的表获取数据：T_PROJECT_INVITE_HOTEL
   - 这是oracle数据库的表，需要根据PROJECT_ID，并且需要联合HTL_INFO.T_HOTEL表中的t.HOTELID, 获取酒店名t.CHN_NAME
4. 实现代码在operate的rfp_management目录下，使用已有用例文件：test_edit_rfp_project_tabs.py，新构建一个用例方法
   - tests/operate/rfp_management/test_edit_rfp_project_tabs.py
5. 注意测试用例层和page形式写法，做到POM形式，元素方法和定位形式分明
    - page在：pages/operate/rfp_management/edit_rfp_project_page.py
6. 使用参数化的形式提取出来，因为里面的传入项目名可能会改变
7. 下方涉及到数据连接的，希望提取成公共类放到utils中，涉及相关查询的操作等

## 步骤描述
1. 首先到/home页
2. 点击菜单:签约管理，选择下拉菜单中选项：签约
3. 然后在：签约的页面中，需要先选择：已启动的Tab项
4. 点击搜索栏目的：签约项目选项中，填入固定项目名：自动化测试项目-价格页签固定酒店项-勿修改任何价格变动
5. 点击搜索按钮，精准匹配出现的首项
6. 接着在首项目的操作栏中，存在编辑按钮：修改，可能存在多个需要精准匹配首个
7. 连接Oracle数据，查找信息，缓存下来
   - 具体的数据连接信息：ip:172.16.88.72, 端口:1521, 服务名:fangcangdb, 用户名htl_rfp, 密码：hf84brfp
8. 其中数据库查询的设计，涉及普通酒店信息（HTL_INFO.T_PROJECT_INTENT_HOTEL这个相关sql查询到的）、集团酒店信息（T_PROJECT_INVITE_HOTEL这个相关sql查询到的）
9. 在修改编辑页面中，选择【邀请酒店】Tab
10. 点击导出按钮，这里导出的是：普通酒店信息
11. 断言：普通酒店信息的Excel中是否存在酒店ID字段，另外对比每一行酒店Id和酒店名，对比在数据库查询表的结果
12. 再点击进入【添加酒店集团意向单店】按钮
13. 再次点击导出按钮：集团酒店信息
14. 断言：集团酒店信息的Excel中是否存在酒店ID字段，另外对比每一行酒店Id和酒店名，对比在数据库查询表的结果


## 以下是我使用playwright工具录制的，你可以参考一下使用，避免自行定位不到可以考虑使用

def run(playwright: Playwright) -> None:
    browser = playwright.chromium.launch(headless=False)
    context = browser.new_context()
    page.get_by_text("签约管理").click()
    page.get_by_text("签约", exact=True).click()
    page.locator("div").filter(has_text=re.compile(r"^已启动$")).first.click()
    page.locator("label").filter(has_text="签约项目").click()
    page.get_by_label("签约项目").fill("hy")
    page.locator(".ml-15 > div").first.click()
    page.get_by_text("修改").nth(1).click()
    page.get_by_role("tab", name="邀请酒店").click()
    with page.expect_download() as download2_info:
        page.locator("div").filter(has_text=re.compile(r"^导出$")).nth(1).click()
    download2 = download2_info.value
    page.get_by_text("添加酒店集团意向单店").click()
    with page.expect_download() as download3_info:
        page.locator("div").filter(has_text=re.compile(r"^导出$")).nth(1).click()

