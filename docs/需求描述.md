# === 1 ===
## 需求描述
需求标题：发布项目
具体描述
1. 通过创建新的项目进行，检测创建项目是否成功;
2. 实现代码在operate的rfp_management目录下，名字：test_create_rfp_project.py
3. 注意测试用例层和page形式写法，做到POM形式，元素方法和定位形式分明
    - page在：pages/operate/rfp_management
4. 使用参数化的形式提取出来，可能存在可变的值



## 步骤描述
1. 实现代码在operate的rfp_management目录下，我们要实现一个功能：发布项目功能：
   - 登录平台端账号，在签约管理顶级菜单：签约管理，选择下拉菜单，发布项目，
   - 项目填写页中，下述是选项框的前项说明，后面输入框/日期选择为必填选项：
     - 签约机构：hy测试机构 （这里会出现一个选择控件，会在这边复现一个下拉框，一般是第一个）
     - 签约项目名称：hyg-自动化项目-{timestamp}
     - 联系人：荷叶
     - 联系电话：17755389215
     - 签约方式：单选框，选择：邀请签约(仅邀请的酒店参与)
     - 报名起止时间、第一轮报价起止时间、协议报价日期范围：这三个都是日历形式的日期选择，三个可以保持一致，选择一个月期限的就可以（开始/结束）
     - 预计签约酒店数量：数量
     - 公司差标最小值
     - 公司差标最大值
   - 点击保存按钮：保存并下一步
   - 断言点击保存是否出现保存成功、操作成功的toast

## 以下是我使用playwright工具录制的，你可以参考一下使用，避免自行定位不到可以考虑使用
## 下方是我之前写的，你可以用作参考

# 导航栏：签约基本信息
self.basic_information_of_contract_locator = page.get_by_role("tab", name="签约基本信息")

# 导航栏：项目介绍
self.project_introduction_locator = page.get_by_role("tab", name="项目介绍")

# 导航栏：签约项目POI
self.project_poi_locator = page.get_by_role("tab", name="签约项目POI")

# 导航栏：邀请酒店
self.project_invite_hotel_locator = page.get_by_role("tab", name="邀请酒店")

# 导航栏：自定义采购策略
self.project_custom_strategy_locator = page.get_by_role("tab", name="自定义采购策略")

# 导航栏：项目采购策略
self.project_purchase_strategy_locator = page.get_by_role("tab", name="项目采购策略")

# 导航栏：权重设置
self.weight_setting_locator = page.get_by_role("tab", name="权重设置")

# 导航栏：酒店白名单
self.hotel_white_locator = page.get_by_role("tab", name="酒店白名单")

# 导航栏：酒店违规配置
self.hotel_violation_locator = page.get_by_role("tab", name="酒店违规配置")

# 导航栏：Lanyon显示设置
self.lanyon_view_locator = page.get_by_role("tab", name="Lanyon显示设置")

# 排序值-输入框
self.sorted_value_input_box_locator = page.locator('//div[text()="排序值"]/../following-sibling::div//input')

# 签约机构-输入框
self.contracting_agency_input_box_locator = page.locator('//div[text()="签约机构"]/../following-sibling::div//input')
self.select_dropdown_item_locator = lambda input_text: page.get_by_text(f"{input_text}", exact=True)

# 签约项目名称-输入框
self.name_of_signed_project_input_box_locator = page.locator('//div[text()="签约项目名称"]/../following-sibling::div//input')

# 联系人-输入框
self.contact_people_input_box_locator = page.locator('//div[text()="联系人"]/../following-sibling::div//input')

# 联系电话-输入框
self.contact_phone_input_box_locator = page.locator('//div[text()="联系电话"]/../following-sibling::div//input')

# 签约方式-单选：邀请签约 / 公开签约
## 测试只能使用邀请签约
self.invitation_to_sign_radio_locator = page.locator('//div[text()="签约方式"]/../following-sibling::div//div[@title="邀请签约(仅邀请的酒店参与)"]/../preceding-sibling::div[@class="c-select-radio"]')
self.public_sign_radio_locator = page.locator('//div[text()="签约方式"]/../following-sibling::div//div[@title="公开签约(所有酒店)"]/../preceding-sibling::div[@class="c-select-radio"]')


# 签约状态通知方式：手工通知 / 自动通知
self.manual_notification_radio_locator = page.locator('//div[text()="签约状态通知方式"]/../following-sibling::div//div[@title="手工通知"]/../preceding-sibling::div[@class="c-select-radio"]')
self.auto_notification_radio_locator = page.locator('//div[text()="签约状态通知方式"]/../following-sibling::div//div[@title="自动通知(议价中、已中签、已拒绝状态自动通知酒店)"]/../preceding-sibling::div[@class="c-select-radio"]')

# 集团报价状态报告：不需要 / 工作日 / 每周通知
self.price_group_not_need_radio_locator = page.locator('//div[text()="集团报价状态报告"]/../following-sibling::div//div[@title="不需要"]/../preceding-sibling::div[@class="c-select-radio"]')
self.price_group_daily_radio_locator = page.locator('//div[text()="集团报价状态报告"]/../following-sibling::div//div[@title="工作日"]/../preceding-sibling::div[@class="c-select-radio"]')
self.price_group_weekly_radio_locator = page.locator('//div[text()="集团报价状态报告"]/../following-sibling::div//div[@title="每周通知"]/../preceding-sibling::div[@class="c-select-radio"]')

# 报名起止时间
self.registration_start_and_end_time_frame_locator = page.locator("//div[text()='报名起止时间']//../following-sibling::div//i[@class='c-animated-arrow noshrink']/preceding-sibling::div")
self.select_time_control_num_locator = lambda day: page.locator(f"//div[@class='c-calendar-body']//div[@class='c-calendar-date-cell-label' and text()='{day}']")

# 第一轮报价起止时间框
self.first_round_registration_start_and_end_time_frame_locator = page.locator("//div[text()='第一轮报价起止时间']//../following-sibling::div//i[@class='c-animated-arrow noshrink']/preceding-sibling::div")

# 协议报价日期范围
self.agreement_quotation_start_and_end_time_frame_locator = page.locator("//div[text()='协议报价日期范围']//../following-sibling::div//i[@class='c-animated-arrow noshrink']/preceding-sibling::div")

# 预计签约酒店数量
self.expect_number_of_contracted_hotels_input_box_locator = page.locator('//div[text()="预计签约酒店数量"]/../following-sibling::div//input')

# 公司差标最小值
self.min_diff_std_of_the_company_input_box_locator = page.locator('//div[text()="公司差标最小值"]/../following-sibling::div//input')

# 公司差标最大值
self.max_diff_std_of_the_company_input_box_locator = page.locator('//div[text()="公司差标最大值"]/../following-sibling::div//input')
