# 问题描述
我希望重写一下run.py，可以做到重新按照指定输入序号来执行相关的

# 重塑需求
1. 当我执行run.py文件的时候，可以终端中出现互动，让我进行选择
- 第一层：提问选择执行环境，后续也在补充选择执行有头或无头模式，在补充allure启用和不启用
  - 第二层：提问选择全部用例执行，标记用例执行，还是指定用例执行
  - 当选择全部用例，就所有用例都运行，自动分配多线程进行
  - 当选择标记用例，列举所有标记进行选择
    - 第三层：按照序号列举划分选择，如选择8. mark_20260507，则执行所有带这个标记的用例
  - 当选择指定用例执行，再继续进行划分
  - 第三层：按角色进行，按模块进行、按文件运行、按用例运行
  - 按角色进行：
    - 第四层：需要列举tests目录下所有目录，并且提供序号让我选择，比如：1. auth, 2.e2e...，选择如1，那么就执行auth下的所有用例
  - 按模块进行：
    - 第四层：需要先列举tests目录下所有目录，提供序号，先让我选择对应角色端后，比如1. auth..., 5.operate，选择了5，接着进入到operate角色层
    - 第五层：需要列举operate这个角色下的搜索模块，比如：1.admin, 2..4.project_management, 5.rfp_management、等，选择5，执行rfp_management模块下所有用例
      - 如果比如在第四层下方，已没有第五层目录了，那么默认就是最后一层，将该第四层目录作为第五层模块计算逻辑
  - 按文件运行：
    - 和模块进行一样，需要先第四层和第五层的询问，接着遍历第五层模块下的所有文件
    - 第六层：选择序号文件，进行执行用例
    - 如果存在中途某一层没有目录，该层就默认为文件计算逻辑
  - 按用例运行：
    - 和文件进行一样，需要先第四层和第五层、第六层询问
    - 需要先询问选中某一个确定文件后
    - 第七层：该测试文件下，每一个测试用例方法的列举

2. 选择某一种状况后，按照该选择的序号状况，进行运行测试用例情况


